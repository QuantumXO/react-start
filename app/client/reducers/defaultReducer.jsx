
const initialState = {
    author: 'quantumXO',
};

export default function defaultReducer(state = initialState, action){
    switch (action.type) {
        default :
            return state;
    }
}
